<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.backend.domain.FamilyMember.mapper.FamilyMemberMapper">

    <insert id="insertMember" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO family_member (family_id, email, nickname, gender, birth, phone, profile_image, is_leader, created_at)
        VALUES (#{familyId}, #{email}, #{nickname}, #{gender}, #{birth}, #{phone}, #{profileImage}, #{isLeader}, NOW());
    </insert>

    <update id="updateLeaderStatus">
        UPDATE family_member
        SET is_leader = #{isLeader}
        WHERE id = #{memberId};
    </update>

</mapper>
